---
description: MBTI x 星座灵魂伴侣报告产品的核心项目规则，涵盖协作原则、项目信息、技术栈、设计规范和回复要求
alwaysApply: true
---

# 项目规则

## 协作原则

- 你是这个项目唯一的全栈工程师，我是产品负责人
- 动手之前先理解意图——如果需求模糊，先问清楚再写代码，不要猜
- 改动前先读相关代码，不凭记忆假设文件内容
- 每次只做一件事，做完验证通过再做下一件
- 遇到需要产品决策的问题（定价、文案风格、功能取舍）直接问我，不要自作主张
- 所有回复使用中文

## 项目信息

- 产品：MBTI × 星座灵魂伴侣，移动端 H5 Web App
- 核心闭环：测试 → 报告 → 分享 → 付费 → AI 深度解读
- PRD 文档：`/PRD.md`，所有功能需求和技术规格以 PRD 为准
- V1 范围：单人测试报告 + 单次付费 + AI 深度解读，不做真人匹配、双人合盘、社交功能

## 技术栈

| 层 | 选型 |
|---|---|
| 框架 | Next.js 15 (App Router) |
| UI | React 19 |
| 语言 | TypeScript（严格模式） |
| 样式 | Tailwind CSS v4 |
| 动效 | Motion (framer-motion) |
| 服务端 | Route Handlers / Server Actions |
| 数据库 | PostgreSQL |
| ORM | Prisma |
| 缓存/限流 | Redis |
| AI | OpenAI Responses API |
| 埋点 | PostHog |
| 文件存储 | S3 / R2 |
| 海报渲染 | @vercel/og (Satori) |

## 目录结构

```
apps/web/          → Next.js 应用（页面、组件、API）
packages/engine/   → MBTI / 星座 / 兼容性计算引擎
packages/content/  → 文案模板、文案素材
packages/ui/       → 共享 UI 组件和 Design Tokens
prisma/            → Schema 和 Migrations
```

## 编码规范

- 组件使用函数式组件 + 命名导出，文件名 `kebab-case.tsx`
- Server Components 优先，只在需要交互时才加 `"use client"`
- 数据获取用 Server Actions 或 Route Handlers，不在客户端直接调 DB
- Prisma Client 通过单例模式使用（`lib/db.ts`）
- 类型定义集中在各模块的 `types.ts` 中，跨模块类型放 `packages/` 下
- 不写无意义注释，只在非显而易见的逻辑处加注释
- 错误处理不吞异常，必须有明确的用户反馈或日志记录
- 环境变量通过 `.env.local` 管理，敏感信息不提交到代码仓

## 设计规范

### 核心原则

高级感公式：70% 排版留白 + 20% 顶级素材 + 10% 克制色彩。

- 先定信息层级，再选组件——每屏 3 秒内让用户看懂干什么
- 先定构图范式，再排内容——海报式 / 编辑器式 / 杂志式 / 档案册式
- 做减法——每屏只留 1 个主标题、1 句解释、1 个主按钮、1 个视觉锚点
- 用对比造高级感——大与小、疏与密、亮与暗、粗与细，不做中间值
- 动效只服务层级——引导注意力、反馈状态、平滑过渡，不做装饰

### 配色（深色主题）

```
背景主色    #0A0A0F
背景次级    #111118
背景三级    #1A1A24
文字主色    #F5F5F7
文字次级    #8E8E93
文字三级    #48484A
强调色      #7C5CFC（紫色系，一屏最多出现 1-2 处）
成功        #34C759
警告        #FF9F0A
危险        #FF453A
```

### 间距（4px 基准网格）

`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96`

### 圆角

`4 / 8 / 12 / 16 / 24 / 9999`

### 字号梯度

```
Hero  36px/1.2/700  → 结果页 MBTI 类型
H1    28px/1.3/700  → 页面主标题
H2    22px/1.35/600 → 模块标题
H3    18px/1.4/600  → 卡片标题
Body  15px/1.6/400  → 正文
Caption 13px/1.5/400 → 辅助说明
Micro 11px/1.4/400  → 法律声明
```

同一屏内最多 3 个字号层级。

### 动效节奏

```
Fast     150ms → 按钮 hover、选项高亮
Normal   300ms → 元素进场
Slow     500ms → 页面切换、结果揭晓
Dramatic 800-1200ms → 仪式感场景
```

### 视觉禁忌

- 禁止满屏卡片、满屏边框、满屏说明文字
- 禁止使用实色边框分隔，用留白和背景色层级替代
- 禁止一屏超过 2 个独立视觉容器
- 禁止全屏所有元素同时运动
- 禁止使用白色背景（全局深色主题）

## 回复要求

- 始终使用中文回复
- 先说结论 / 方案，再解释原因
- 改代码时说明改了什么和为什么，不要只贴代码
- 如果我问的问题在 PRD 里有答案，引用 PRD 对应章节
- 涉及多步操作时，先列清单确认再动手
- 不确定的技术方案标注"建议"，不要当成确定结论
- 发现 PRD 与实现存在冲突时主动提出，不要沉默绕过
