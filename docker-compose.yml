services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mbti
    ports:
      - "5432:5432"
    volumes:
      - mbti_pg_data:/var/lib/postgresql/data

volumes:
  mbti_pg_data:
